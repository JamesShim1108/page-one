# Convenience feature series: Batch 00 baseline

**Purpose:** read-only preflight for the 18-batch convenience-feature series.  
**Baseline inspected:** `57a3f1c` (`feat: add Unit 2 definitions and Algebra 2 shell`).  
**Series branch:** `convenience-features` (created from the complete current Unit 2/Algebra 2 baseline).  
**Status:** Batch 00 complete; no feature implementation has started.

## Branch and worktree

- The preflight began on `unit-2-networks-of-exchange`.
- The worktree was clean before this report was added.
- Local `main` and `origin/main` pointed to `ffb042b`; they were not modified.
- The current baseline was two commits ahead of `origin/unit-2-networks-of-exchange` and includes the completed Unit 2 and Algebra 2 work needed by the future batches.
- All 18 future implementation batches will use this one branch. They are not separate Git branches. Each later batch may have its own commit on this branch, subject to the user's instruction at that time.

## Instructions and scripts

No `AGENTS.md` file was found in the repository or its parent workspace during the preflight. The executor must repeat this check at the start of each batch.

The inspected `package.json` scripts are:

| Command | Purpose |
|---|---|
| `npm run build` | Build generated content |
| `npm run validate` | Build/check generated content |
| `npm test` | Run `tests/*.test.mjs` |
| `npm run format:check` | Check Prettier formatting |
| `npm run check` | Validate, run all tests, and check formatting |
| `npm run image` | Prepare an image asset |

The series should use targeted tests for each batch and any repository-required gate. It should not run every test after every small edit merely for volume. Existing failures must be recorded before attribution.

## Current architecture relevant to the series

| Area | Inspected locations |
|---|---|
| Build/discovery/validation | `scripts/build-content.mjs`, `scripts/lib/validate.mjs`, `scripts/lib/glossary.mjs`, `scripts/lib/terms.mjs` |
| Runtime routing/loading | `docs/app/router.js`, `docs/app/content-store.js`, `docs/app/main.js` |
| Reading/definitions | `docs/app/blocks.js`, `docs/app/views/lesson.js`, `docs/app/views/guide.js`, `docs/app/concepts/` |
| Quiz | `docs/app/quiz/engine.js`, `store.js`, `controller.js`, `views.js` |
| Terms | `docs/app/terms/engine.js`, `store.js`, `controller.js`, `views.js` |
| Writing | `docs/app/writing/engine.js`, `store.js`, `controller.js`, `views.js` |
| Catalog/course shell | `docs/app/views/catalog.js`, `docs/content/algebra-2/course.js` |
| Existing tests | `tests/concepts.test.mjs`, `content-system.test.mjs`, `critical-spec.test.mjs`, `terms.test.mjs`, `unit2.test.mjs` |

Important observations: quiz, writing, and Terms stores currently use tab-scoped `sessionStorage`; the route loader already guards against stale asynchronous loads; Terms already preserves a frozen filtered round and saved order; the definition system already has a shared controller/positioner; and Algebra 2 is an empty `soon` shell. These are facts to preserve or evolve deliberately, not permission to rewrite the architecture.

## Protected-content inventory

Before each implementation batch, compare the relevant files against the commit before that batch. The following are protected unless a batch explicitly authorizes noninstructional metadata or a validated schema field:

- Official Terms: `docs/content/world-history/terms/**/*.js`.
- Existing World History lesson text and reading guides: `docs/content/world-history/unit-*/topics/**/lesson.js`, `study-guide.js`, and unit metadata.
- Existing question banks and answers: `docs/content/world-history/unit-*/topics/**/questions.js`, unit practice activity sources, and generated bank/quizzes output.
- Writing prompts/rubrics: `docs/content/world-history/unit-*/writing.js` and generated writing output.
- Source records and glossary definitions: `docs/content/world-history/sources.js`, glossary modules, and generated glossary selections.
- Algebra 2 learning content: everything under `docs/content/algebra-2/` other than its already-existing honest empty-shell metadata; no new instructional files are permitted in this series.

Generated JSON under `docs/generated/` must be regenerated by the build when its source changes. It must never be hand-edited as a parallel source of truth.

## Batch 00 result and next prerequisite

The repository is suitable for Batch 01. No source PDFs were needed for this preflight because Batch 00 does not author or verify historical content. Batch 01 may begin only under a separate user authorization and must implement the durable storage foundation described in `execution/01-storage-foundation.md`; it must not start conflicts, writing UI, quiz redesign, or offline support in the same run.

This report is the only repository change made by Batch 00. No feature, new course content, Terms set, question, source claim, commit to `main`, merge, push, or deployment was performed by the preflight itself.
